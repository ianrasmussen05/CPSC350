#include <iostream>

using namespace std;

class Quiz1
{
public:
  Quiz1(); // Constructor
  ~Quiz1(); // Destructor, used to free up memory
  int countVowels(string input);
private:
  // There are none
};
